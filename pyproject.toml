[project]
name = "mte-crypto-hunter"
version = "0.1.0"
description = "Causal Binance altcoin pre-explosion research and live scanner"
requires-python = ">=3.11"
dependencies = [
  "joblib>=1.3",
  "numpy>=1.26",
  "pandas>=2.0",
  "scikit-learn==1.8.0",
  "websockets>=16.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["mte_crypto*"]
